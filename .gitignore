node_modules
dist
dist-ssr
*.local
src-tauri/target
src-tauri/gen
.DS_Store
.vscode/*
!.vscode/extensions.json
rust-install.log
rust-version.txt

# Local build tooling mirror (WiX/NSIS cache + dev helper scripts; contains
# machine-specific paths and large binaries - not part of the source).
.tauri-mirror/

# Dev / build logs.
*.log

# User data (collected raw materials live here, not in source control).
RawMaterialCollector/

# Session handoff doc (transient, written for AI sessions, not project docs).
docs/HANDOFF.md
